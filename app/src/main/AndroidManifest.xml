<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sushanth.shopping_mall">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <!--<activity android:name=".GridViewBaseAdapter" />-->
        <activity android:name=".Home_Grid" />
        <activity android:name=".funzone" />

        <!--<activity android:name=".Main2Activity" />-->
        <activity android:name=".Registration" />
        <activity android:name=".Boxoffice" />
        <activity android:name=".Admin" />
        <activity android:name=".success" />
        <activity android:name=".categories" />
        <activity android:name=".sub"></activity>
    </application>

</manifest>